export const environment = {
  production: false,
  apiUrl: 'http://localhost:5000/api', // URL do backend .NET Core (quando integrado)
  mockData: true, // Usar dados mockados (true) ou API real (false)
  version: '1.0.0'
};
